# Cloudflare Pages Configuration
# This file is optional but can be used for advanced Cloudflare Pages settings
# Most settings are configured via the Cloudflare Dashboard

name = "kevina-cosmetics"
compatibility_date = "2024-01-01"

[env.production]
name = "kevina-cosmetics"

# Build configuration (also set in Cloudflare Dashboard)
# build_command = "npm run build"
# build_output_dir = "dist"

